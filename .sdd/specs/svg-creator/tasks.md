# 実装タスクリスト
[調査結果](C:\Users\taiga\source\repos\SvgCreator\.sdd\specs\svg-creator\research-python-code-structure.md)と、設計を元に実装を行うこと

## セクション1：データモデル実装
- [x] 1.1 必要な型定義・データ構造を作成する
  - design.mdで定義された `ImageData`, `ShapeLayer`, `DepthOrder`, `Path`, `PathSegment`, `QuantizationResult` などのモデルを実装する
  - 凸性・非交差といったバリデーションをガードまたは契約として実装する
- [x] 1.2 デバッグスナップショットモデルを実装する
  - `DebugSnapshot` ファミリー（Image/Layer/Mask、`CliOptions`、`Version`）と JSON シリアライザを整備する
  - バージョン互換チェックの実装と単体テストを追加する
- [x] 1.3 デバッグ出力レイアウト仕様を固める
  - `metadata.json` で用いるファイルメタ情報のスキーマを確定する
  - `pipeline.json`, `layers.json`, 追加アセット（画像ダンプ等）の命名・配置規則を設計し、ヘルパーで表現できるようにする

## セクション2：ビジネスロジック実装
- [x] 2.1 Orchestrator（CLIコア）の処理を実装する
  - design.mdの処理フロー ステップ1-3（CLI引数解析〜進捗表示）に対応する
  - `IDebugSink` を組み込み、ステージ完了時にスナップショットを送出できるようにする
- [x] 2.2 ImageReader / Preprocessor を実装する
  - design.mdの処理フロー ステップ2-3（入力読込・色空間変換・前処理）に対応する
  - Windows 11 と Ubuntu 22.04 の両方で動作するよう `OpenCvSharp4.runtime.any` などのランタイムパッケージ選定と依存登録を行い、OS依存箇所を排除する
- [x] 2.3 Quantizer（K-means減色）を実装する
  - design.mdの処理フロー ステップ4（量子化）に対応し、ML.NETベースのK-meansを構成する
- [x] 2.4 OpenCvSharp のクロスプラットフォーム対応を整備する
  - Windows 固有の API やパス依存を排除し、Linux でも同一の処理が動作するよう ImageReader/Preprocessor の実装をリファクタリングする
  - `OpenCvSharp4.runtime.any` もしくは OS 別ランタイムの導入手順をコードコメント/設定に反映し、ビルド定義を更新する
- [x] 2.5 ShapeLayerBuilder（領域抽出）を完成させる
  - [x] 2.5.1 外周輪郭トレースを実装し、連結成分境界を CCW ポリラインで生成する
  - [x] 2.5.2 穴領域（Holes）を検出し `ShapeLayer.Holes` を構築する
    - [x] 2.5.2.1 輪郭階層とポリゴン包含条件を整理し検出フローを明文化する
    - [x] 2.5.2.2 OpenCvSharp の階層出力から穴候補を抽出するヘルパーを実装する
    - [x] 2.5.2.3 抽出結果を `ShapeLayer.Holes` へマッピングし外周との整合を確認する
  - [x] 2.5.3 微小成分の除去ロジック（`noisyThreshold`）を導入する
    - [x] 2.5.3.1 design.md のノイズ基準を読み合わせし閾値ルールを決定する
    - [x] 2.5.3.2 連結成分ごとの面積・周長フィルタを実装する
    - [x] 2.5.3.3 フィルタ結果をレイヤ構築フローへ反映し副作用を点検する
  - [x] 2.5.4 ノイズ層を `NoisyLayer[]` として分類・返却する
    - [x] 2.5.4.1 `NoisyLayer` に保持すべき情報と初期化手順を決める
    - [x] 2.5.4.2 除外済み成分から `NoisyLayer` を生成する処理を実装する
    - [x] 2.5.4.3 呼び出し側 API にノイズ層返却を組み込みハンドリングを確認する
  - [x] 2.5.5 境界/穴/ノイズケースを網羅する単体テストを追加する
    - [x] 2.5.5.1 境界と穴を含むテストフィクスチャを準備する
    - [x] 2.5.5.2 `noisyThreshold` の境界値ケースを再現するテストを作成する
    - [x] 2.5.5.3 ノイズ層返却と穴構築の期待値を検証するテストを追加する
- [x] 2.6 DepthOrdering（深度整序）を実装する
  - design.mdの処理フロー ステップ6（隣接グラフ算出・SCC縮約・トポロジカルソート）に対応する
  - [x] 2.6.1 隣接グラフと SCC のデータ構造を設計し仕様を固める
  - [x] 2.6.2 形状の前後関係から隣接グラフを構築する処理を実装する
  - [x] 2.6.3 SCC 縮約とループ検出のアルゴリズムを実装する
  - [x] 2.6.4 トポロジカルソートで深度順を決定し `DepthOrder` を生成する
  - [x] 2.6.5 深度競合ケースの単体テストを追加する
- [ ] 2.7 OcclusionCompleter（エラスティカ補完）を実装する
  - design.mdの処理フロー ステップ7（遮蔽部補完）に対応し、Math.NET＋自前L-BFGSで最適化する
  - [ ] 2.7.1 エネルギー関数と制約条件を設計ドキュメントと突き合わせる
  - [ ] 2.7.2 Math.NET と L-BFGS のセットアップコードを整備する
  - [ ] 2.7.3 遮蔽輪郭の補完計算を実装し境界条件を確認する
  - [ ] 2.7.4 Orchestrator への組み込みとデータ受け渡しを調整する
  - [ ] 2.7.5 簡易ケースで収束を確認する検証テストを追加する
- [ ] 2.8 BezierFitter（曲線近似）を実装する
  - design.mdの処理フロー ステップ8（Bézierフィット・単純化）に対応する
  - [ ] 2.8.1 ポリラインから Bézier へ変換する方針を確定する
  - [ ] 2.8.2 誤差許容値と分割条件のロジックを実装する
  - [ ] 2.8.3 制御点計算ロジックを実装し数値安定性を確認する
  - [ ] 2.8.4 近似後の単純化・平滑化処理を追加する
  - [ ] 2.8.5 代表輪郭で近似品質を検証するテストを作成する
- [ ] 2.9 SvgEmitter（SVG出力）を実装する
  - design.mdの処理フロー ステップ9（`<svg>/<g>/<path>` 生成と属性最適化）に対応する
  - [ ] 2.9.1 SVG ルートとビュー設定の生成ロジックを設計する
  - [ ] 2.9.2 レイヤー構造を `<g>` にマッピングする処理を実装する
  - [ ] 2.9.3 `<path>` 属性のフォーマット最適化（小数桁・冗長属性）を実装する
  - [ ] 2.9.4 サンプルデータで生成結果を確認するスモークテストを実行する
- [ ] 2.10 ExportFilter / SizeLimiter を実装する
  - design.mdの処理フロー ステップ9（レイヤ別出力と15KB制約対応）に対応する
  - [ ] 2.10.1 `--export-layer` と出力対象の対応表を整理する
  - [ ] 2.10.2 ExportFilter のフィルタリング処理を実装する
  - [ ] 2.10.3 SizeLimiter の閾値計算と圧縮処理を実装する
  - [ ] 2.10.4 フィルタとサイズ制限の組み合わせを検証するテストを追加する
- [ ] 2.11 デバッグ出力シンク（File/Null）を実装する
  - `out/debug/` レイアウトとファイル命名を 1.3 の仕様に従って実装する
  - `--debug-dir`, `--debug-stages`, `--debug-keep-temp` に応じたフィルタリングやクリーンアップを実現する
  - [ ] 2.11.1 出力レイアウトと命名ルールの実装計画を文書化する
  - [ ] 2.11.2 File シンクとディレクトリ生成処理を追加する
  - [ ] 2.11.3 Null シンク実装と選択ロジックを追加する
  - [ ] 2.11.4 デバッグオプション組み合わせで動作確認するテストを実装する
- [ ] 2.12 エラーハンドリングを実装する
  - design.mdに列挙されたエラーケース（入力不正・未収束・サイズ超過等）の処理を網羅する
  - デバッグ出力失敗・バージョン不一致時の例外処理とログ通知を追加する
  - [ ] 2.12.1 エラーケースを分類し対処方針を整理する
  - [ ] 2.12.2 共通エラー型とメッセージ生成ロジックを実装する
  - [ ] 2.12.3 各コンポーネントからの例外伝播と CLI 応答を調整する
  - [ ] 2.12.4 代表的な失敗入力でエラーハンドリングを検証するテストを追加する

## セクション3：インターフェース実装
- [ ] 3.1 CLIコンポーネントを作成する
  - `svg-creator` メインコマンドとサブコマンド/オプションを System.CommandLine で定義する
  - [ ] 3.1.1 CLI オプションとコマンド構成を design.md と突き合わせる
  - [ ] 3.1.2 ルートコマンドと共通オプションを実装する
  - [ ] 3.1.3 サブコマンドとハンドラを Orchestrator に接続する
  - [ ] 3.1.4 パース結果を検証する単体テストを追加する
- [ ] 3.2 入力バリデーションを実装する
  - ファイルパス存在確認や `--quality`, `--threads`, `--export-layer` などの値域チェックを実装する
  - [ ] 3.2.1 必須パラメータと値域チェックの要件を洗い出す
  - [ ] 3.2.2 ファイルパスと存在確認ロジックを実装する
  - [ ] 3.2.3 数値オプションの範囲・相互依存を検証する処理を追加する
  - [ ] 3.2.4 バリデーション失敗時のエラーメッセージをテストで確認する
- [ ] 3.3 出力フォーマットを実装する
  - 単一SVGとレイヤ別SVGのファイル命名・メタデータ（`layer-XXX.svg`, `data-depth` 属性）を整える
  - [ ] 3.3.1 出力命名規則とメタデータ仕様を整理する
  - [ ] 3.3.2 単一 SVG 出力の生成処理を実装する
  - [ ] 3.3.3 レイヤ別 SVG 出力と `data-depth` 属性付与を実装する
  - [ ] 3.3.4 出力ファイルセットを検証するテストを追加する
- [ ] 3.4 デバッグ関連オプションを実装する
  - `--debug`, `--debug-dir`, `--debug-stages`, `--debug-keep-temp` を定義し、値検証と Orchestrator へのバインドを行う
  - [ ] 3.4.1 デバッグオプションの組み合わせ要件を整理する
  - [ ] 3.4.2 System.CommandLine にデバッグオプションを追加する
  - [ ] 3.4.3 オプション値を Orchestrator にバインドする処理を実装する
  - [ ] 3.4.4 オプション組み合わせの受け渡しを検証するテストを追加する

## セクション4：統合とテスト
- [ ] 4.1 コンポーネントを統合する
  - Orchestrator に全コンポーネントを配線し、データフローとスレッド制御を確認する
  - [ ] 4.1.1 依存グラフと初期化順序を整理する
  - [ ] 4.1.2 実装済みコンポーネントを Orchestrator に配線する
  - [ ] 4.1.3 小規模入力で統合フローを手動実行しログを確認する
  - [ ] 4.1.4 統合手順と注意点をドキュメント化する
- [ ] 4.2 クロスプラットフォーム依存性を検証する
  - Windows 11 / Ubuntu 22.04 想定環境で OpenCvSharp を含むネイティブ依存が解決されることを確認し、ファイルパスや一時ディレクトリの処理が OS 非依存であることをテストする
  - [ ] 4.2.1 Windows / Ubuntu 向け依存ライブラリの解決手順をリスト化する
  - [ ] 4.2.2 Windows 環境での実行確認とパス依存の洗い出しを行う
  - [ ] 4.2.3 Ubuntu 環境での実行確認と一時ディレクトリ処理を検証する
  - [ ] 4.2.4 差異と対処法を README 用メモにまとめる
- [ ] 4.3 基本的な動作テストを実装する
  - 単体テスト（Quantizer, DepthOrdering, BezierFitter, SizeLimiter 等）と統合テスト（小規模入力で要求満足）を作成する
  - [ ] 4.3.1 単体テスト対象コンポーネントとケースを一覧化する
  - [ ] 4.3.2 Quantizer / DepthOrdering の単体テストを実装する
  - [ ] 4.3.3 BezierFitter / SizeLimiter の単体テストを実装する
  - [ ] 4.3.4 小規模入力の統合テストを追加し CI に組み込む
- [ ] 4.4 デバッグ出力の統合テストを実装する
  - 一時ディレクトリに `pipeline.json`, `layers.json`, `metadata.json` が生成され、要件8（バージョン・CLIオプション写し・ステージ絞り込み）が満たされることを検証する
  - [ ] 4.4.1 生成すべきデバッグファイルセットと期待値を一覧化する
  - [ ] 4.4.2 `--debug-stages` 指定時の統合テストを実装する
  - [ ] 4.4.3 `--debug-keep-temp` の振る舞いを確認するテストを実装する
  - [ ] 4.4.4 テスト結果を踏まえて比較用 JSON を整備する
- [ ] 4.5 要件の受入基準を満たすことを確認する
  - requirements.md の要件（凸性チェック、レイヤ別出力、進捗表示、CLI挙動、デバッグ出力）に対する受入確認を記録する
  - [ ] 4.5.1 requirements.md の各項目をチェックリスト化する
  - [ ] 4.5.2 手動・自動テストの検証結果を記録する
  - [ ] 4.5.3 未充足項目のフォローアップタスクを洗い出す
  - [ ] 4.5.4 受入確認ログをリポジトリに残す
- [ ] 4.6 README でネイティブ依存の導入手順を記載する
  - ランタイムパッケージの選択理由と導入手順を README に追記し、プラットフォーム固有の設定が不要である旨を明示する
  - [ ] 4.6.1 README 追記箇所のアウトラインを作成する
  - [ ] 4.6.2 Windows 向け導入手順と依存解決メモを記述する
  - [ ] 4.6.3 Ubuntu 向け導入手順と注意事項を記述する
